(defpoll display_workspaces :interval "70ms" :run-while true
	 `$HOME/.config/eww/scripts/workspaces`
	 )


(defwidget workspaces []
	   (box
	   (literal :valign "center" :content display_workspaces)
	    )
	   )
