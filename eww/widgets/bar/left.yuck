(defpoll display_workspaces :interval "70ms" :run-while true `$HOME/.config/eww/scripts/workspaces`)
(defwidget workspaces [] (literal :content display_workspaces))

(defwidget left[]
 (box
  :space-evenly false
  :orientation "v"
  :class "left"

  :spacing 10

  (workspaces)
 )
 )
